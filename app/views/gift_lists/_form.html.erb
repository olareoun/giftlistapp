<%= form_for(@gift_list) do |f| %>
  <% if @gift_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gift_list.errors.count, "error") %> prohibited this gift_list from being saved:</h2>

      <ul>
      <% @gift_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
