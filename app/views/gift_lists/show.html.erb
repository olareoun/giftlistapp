<%- model_class = GiftList -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:title) %>:</strong></dt>
  <dd><%= @gift_list.title %></dd>
  <dt><strong><%= model_class.human_attribute_name(:description) %>:</strong></dt>
  <dd><%= @gift_list.description %></dd>
</dl>

<%= render :partial => 'gifts/gifts', :locals => {:gifts => @gift_list.gifts} %>

<div class="form-actions">
  <%= link_to t('.edit', :default => t("New gift")),
              new_gift_list_gift_path(@gift_list), :class => 'btn' %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_gift_list_path(@gift_list), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              gift_list_path(@gift_list),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
  <%= link_to t('.back', :default => t("helpers.links.back")),
              gift_lists_path, :class => 'btn'  %>
</div>
